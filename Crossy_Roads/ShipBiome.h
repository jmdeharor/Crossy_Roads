#pragma once
/*#include "Biome.h"

class ShipBiome : public Biome {
public:
	void initRoad(std::vector<glm::uint>& adjacentRow, const std::vector<CellProperties>& map) override;
	void initSafeZone(const std::vector<CellProperties>& map) override;
	ShipBiome(RowVectors& vectors);
	~ShipBiome();
};
*/
