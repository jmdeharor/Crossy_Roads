#pragma once
/*#include "Biome.h"

class SeaBiome : public Biome {
public:
	void initRoad(std::vector<glm::uint>& adjacentRow, const std::vector<CellProperties>& map) override;
	SeaBiome(RowVectors& vectors);
	~SeaBiome();
};*/

